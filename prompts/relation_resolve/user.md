Predicate vocabulary:
{relations}

Phrases to canonicalize:
{phrases_json}

Return strict, valid JSON with exactly one field:

{{
  "resolved": [
    {{"phrase": "...", "canonical": "one of the predicates above, or null"}}
  ]
}}

Include exactly one entry per phrase given above, in the same order, using the phrase text
exactly as given.
